import {
  colorThemeKey,
  darkThemeKey,
  lightThemeKey,
} from '@/constants/calculator';

const themeOptions = [
  { value: lightThemeKey, text: 'Light Theme' },
  { value: darkThemeKey, text: 'Dark Theme' },
  { value: colorThemeKey, text: 'Color Theme' },
];

export default themeOptions;
